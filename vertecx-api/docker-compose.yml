services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vertecx-api
    env_file:
      - .env.docker
    ports:
      - "3001:3001"
    restart: unless-stopped
  